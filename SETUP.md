# מדריך הפעלה - Tico מערכת שיווק אוטומטית

המערכת בנויה ומוכנה בקוד, אבל היא לא תעבוד עד שתחברו אותה לחשבונות שלכם.
המדריך הזה עובר על כל חשבון/מפתח שצריך, ולפי מה הוא משמש. מומלץ להתחיל
בסעיף 1 (אימות עסקי של מטא) כי הוא היחיד שיכול לקחת מספר ימים - כל
השאר אפשר להכין תוך כמה דקות במקביל.

## מה המערכת עושה בפועל

1. אחת לשבוע (או בלחיצת כפתור בפאנל) המערכת יוצרת טיוטות פוסטים ומיילים
   לפי נושאים, בעזרת Claude.
2. הטיוטות **לא** מתפרסמות אוטומטית - הן מחכות באזור "טיוטות לאישור"
   בפאנל הניהול.
3. אתם עוברים על כל טיוטה, עורכים אם צריך, מסמנים ששני תנאי הציות
   מתקיימים, ולוחצים "אישור ופרסום" / "אישור ושליחה".
4. רק אז זה יוצא בפועל לפייסבוק/אינסטגרם או נשלח לרשימת התפוצה.

## סדר מומלץ להקמה

### 1. Vercel + בסיס נתונים (5 דקות)

- צרו חשבון ב-vercel.com וחברו את הריפו הזה כפרויקט.
- הוסיפו Postgres דרך Vercel (Storage → Postgres) או ב-neon.tech - שני
  אלה עובדים, פשוט תעתיקו את `DATABASE_URL` לתוך משתני הסביבה של הפרויקט.
- אחרי חיבור מסד הנתונים, הריצו מקומית או ב-Vercel:
  `npm run db:push` (יוצר את כל הטבלאות מהסכמה).

### 2. Anthropic API key (יצירת התוכן) - כמה דקות

- console.anthropic.com → API Keys → צרו מפתח.
- שימו ב-`ANTHROPIC_API_KEY`. יש כרטיס אשראי לחייב מעבר לקרדיט חינמי -
  העלויות כאן זניחות (כמה דולרים בחודש כתלות בקצב הפרסום).

### 3. חשבון Resend + אימות דומיין (כמה דקות עד יום, תלוי DNS)

- resend.com → הרשמה → Domains → הוסיפו את הדומיין שלכם.
- הוסיפו את רשומות ה-DNS (SPF/DKIM) שResend נותן לכם אצל ספק הדומיין.
  זה יכול לקחת כמה דקות עד כמה שעות עד שהDNS מתעדכן.
- צרו API key ושימו ב-`RESEND_API_KEY`.
- `RESEND_FROM_EMAIL` צריך להיות כתובת מהדומיין המאומת, למשל
  `"השם שלכם <marketing@yourdomain.com>"`.

### 4. עמוד פייסבוק + חשבון אינסטגרם עסקי (כמה דקות)

- אם אין לכם עדיין עמוד פייסבוק לעסק - צרו אחד.
- הפכו את חשבון האינסטגרם שלכם ל"חשבון עסקי"/"יוצר" (בהגדרות
  האינסטגרם) וחברו אותו לעמוד הפייסבוק (Meta Business Suite → הגדרות
  → חשבונות מקושרים).

### 5. אפליקציית Meta Developer + אימות עסקי (**הכי לוקח זמן - להתחיל מוקדם**)

- developers.facebook.com → צרו אפליקציה מסוג Business.
- Meta Business Suite → הגדרות עסקיות → אימות עסקי (Business
  Verification) - זה יכול לקחת בין יום לשבועיים, תלוי בבדיקת
  המסמכים מצד מטא. **תתחילו בזה מיד**, כל שאר הסעיפים לא תלויים בזה.
- לאחר האישור, צרו System User (במקום טוקן אישי שפג תוקף) עם הרשאות:
  `pages_manage_posts`, `pages_read_engagement`, `instagram_basic`,
  `instagram_content_publish`, ותייצרו לו Access Token ארוך טווח.
- `META_PAGE_ID` - המזהה של עמוד הפייסבוק (Graph API Explorer → `me/accounts`).
- `META_IG_USER_ID` - מזהה חשבון האינסטגרם המקושר (`{page-id}?fields=instagram_business_account`).
- `META_PAGE_ACCESS_TOKEN` - הטוקן שיצרתם.

### 6. Vercel Blob (תמונות לפוסטים באינסטגרם)

- Vercel → Storage → Blob → צרו Store. זה ייתן `BLOB_READ_WRITE_TOKEN`
  אוטומטית אם הפרויקט מחובר ל-Vercel; אם עובדים מקומית, העתיקו אותו
  מהגדרות הפרויקט. אינסטגרם לא תומך בפוסט טקסט בלבד - חובה תמונה, אז
  זה נדרש לפני שאפשר לאשר פוסט לאינסטגרם.

### 7. תבניות תמונה (כדי שהפוסטים יהיו מוכנים לגמרי אוטומטית, כולל תמונה)

בברירת מחדל המערכת יוצרת רק את הטקסט - התמונה נשארת ריקה וצריך להעלות
ידנית בכל פוסט. כדי לסגור את הפער: היכנסו ל"תבניות תמונה" בפאנל
והעלו כמה תמונות רקע קבועות שלכם (תמונת פרופיל, המשרד, וכו&apos;). מרגע
שיש תבנית פעילה אחת לפחות, כל טיוטת פוסט חדשה תקבל אוטומטית תמונה -
עם הכותרת הראשית של הפוסט מעוצבת עליה (טקסט לבן על גרדיאנט בצבעי
המותג) - בלי שתצטרכו להעלות תמונה בכל פעם. אפשר תמיד להחליף תמונה
ידנית לפוסט ספציפי בפאנל האישור.

### 8. משתני סביבה נוספים

- `AUTH_SECRET` - `openssl rand -base64 33`.
- `ADMIN_EMAIL` + `ADMIN_PASSWORD_HASH` - זה הכניסה שלכם לפאנל. הריצו
  `node -e "console.log(require('bcryptjs').hashSync('הסיסמה-שלכם', 10))"`
  ושימו את התוצאה ב-`ADMIN_PASSWORD_HASH`.
- `CRON_SECRET` - כל מחרוזת אקראית (Vercel שולח אותה חזרה כ-Header
  לאימות שהקריאה השבועית באמת מגיעה מ-Vercel Cron).
- `APP_URL` - כתובת הפרויקט ב-Vercel (נדרש לקישורי הסרה מרשימת תפוצה).
- `RTM_API_SECRET` - אופציונלי, רק אם רוצים לקרוא ל-`/api/rtm/briefs`
  (בריפים יומיים לסרטוני אינסטגרם, ר' בהמשך) מכלי חיצוני ולא מהפאנל.
- ראו את `.env.example` לרשימה המלאה.

### RTM - בריפים יומיים מהחדשות

מודול נפרד (`/admin/rtm`) שסורק אחת ליום את Ynet, גלובס וכלכליסט
בחיפוש ידיעות בנושאי משכנתאות/ריבית/נדל"ן, ומכין לכל ידיעה בריף
ל-AI לסרטון: מה קרה, מה זה אומר למחזיקי משכנתא, ושאלת סיום. לא דורש
חשבון נוסף - רק את `ANTHROPIC_API_KEY` ואת `DATABASE_URL` שכבר
הוגדרו למעלה. כתובות ה-RSS ב-`lib/rtm-news-sources.ts` הן best-effort
(לא נבדקו מול האתרים החיים) - כדאי לוודא אחרי הפריסה הראשונה, בלחיצה
על "סרוק חדשות עכשיו" בפאנל, שאכן חוזרות ידיעות; אם לא, לעדכן את
כתובת ה-RSS הרלוונטית באותו קובץ.

## אחרי שהכל מחובר

1. היכנסו לפאנל בכתובת `/admin/login`.
2. ב"תבניות תמונה" העלו כמה תמונות רקע (מומלץ לפני היצירה הראשונה, כדי
   שהפוסטים ייצאו כבר עם תמונה).
3. עברו ל"נושאים" ולחצו "צור טיוטות עכשיו" ליצירת סבב ראשון של תוכן.
4. עברו ל"טיוטות לאישור", ערכו במידת הצורך, סמנו את תנאי הציות, ואשרו.
5. ב"רשימת תפוצה" ייבאו קובץ CSV עם לקוחות עבר (עמודה בשם `email`,
   ואופציונלית `first_name`/`last_name`).
6. הקרון השבועי (יום שני 06:00, ניתן לשנות ב-`vercel.json`) ימשיך
   ליצור טיוטות חדשות אוטומטית - אבל תמיד ידרוש את האישור שלכם לפני
   שמשהו יוצא לאוויר.

## נקודות משפטיות לבדוק מול עורך דין (לא ייעוץ משפטי)

- **תיקון 40 לחוק התקשורת (חוק הספאם)**: מייל שיווקי בישראל מחייב
  זיהוי שולח וקישור הסרה מיידי וחינמי (קיים כאן), וכן הסכמה מפורשת או
  החרגה צרה למי שהיה "לקוח קיים" - כדאי לוודא עם עו"ד שרשימת הלקוחות
  שלכם עומדת בהחרגה ולכמה זמן אחרי סיום ההתקשרות.
- אם יש נמענים באיחוד האירופי, ייתכן שגם **GDPR** רלוונטי.
- איסוף ואחסון מידע אישי (מיילים, שמות) עשוי לחייב רישום מאגר מידע
  לפי **חוק הגנת הפרטיות** - שווה בדיקה קצרה מול עו"ד בהתאם לגודל
  הרשימה.
- תחום ייעוץ המשכנתאות מפוקח - התוכן צריך להישאר כללי-חינוכי, בלי
  הבטחות ריבית/תוצאה, ועם המשפט המבהיר שזה לא ייעוץ מחייב. תיבות
  הסימון בפאנל האישור נועדו לעזור לכם לזכור לבדוק את זה בכל פריט.
